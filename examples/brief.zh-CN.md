# Mock Brief 示例

> 这是脱敏示例，用于展示 Lark Resume 的输出形态。人物、公司、项目和数据均为虚构内容。

## 综合定位

Agent 基础设施与应用开发工程师，长期围绕工具调用、运行时编排、评测链路和企业集成，把不稳定的 LLM 能力封装成可观测、可回放、可交付的工程系统。

## 履历概括

2023-06 至今在某 AI 平台团队负责 Agent runtime 与开发者工具链建设，覆盖工具协议接入、任务调度、执行沙箱、评测数据集和线上问题排查。工作重心从单个 Agent 应用开发逐步扩展到通用运行时能力和工程质量体系。

## 职业能力概括

- 能把 Agent 的规划、工具调用、状态管理和错误恢复拆成可测试的工程模块，降低复杂行为的不可控性。
- 能围绕真实线上问题建立日志、回放和评测链路，让 Agent 迭代从主观观察转为可复现分析。
- 能在产品、平台和客户集成之间做技术取舍，把原型能力推进到稳定交付。

## 代表项目

| 项目 | 项目背景 | 个人价值 |
| --- | --- | --- |
| ⭐ Agent Runtime 执行框架 | 多类 Agent 应用需要统一的任务生命周期、工具调用和状态恢复能力。 | - 设计任务队列、step 状态机和工具调用结果归档结构。<br>- 引入可回放执行日志，支持失败定位和行为复盘。<br>- 将一次性 demo 代码沉淀为可复用 runtime 模块。 |
| ⭐ Tool Calling 网关 | 外部 API、内部服务和本地脚本需要以统一方式暴露给 Agent。 | - 抽象工具 schema、鉴权、超时、重试和错误格式。<br>- 支持按环境隔离工具权限，减少越权调用风险。<br>- 为开发者提供本地调试和线上调用一致的接入方式。 |
| Agent 评测流水线 | Agent 改动难以用传统单元测试覆盖，需要持续评估任务成功率和回归风险。 | - 构建基于真实任务样例的评测集和评分规则。<br>- 接入执行轨迹、模型输出和工具结果对比。<br>- 帮助团队在发布前发现规划偏移、工具误用和格式回归。 |

## 待确认

请确认上述职业定位、项目背景和个人价值点是否准确；如果要生成正式简历，还需要确认目标岗位、技术栈细节和可公开的项目指标。
